# 项目仿写总览

## 1. 项目定位

{用简洁文字说明这个项目解决什么问题、面向什么用户/场景、核心业务价值是什么。避免展开成产品方案，只写理解项目和仿写方向所必需的信息。}

## 2. 服务与模块地图

| 服务 | 模块 | 主要职责 | 依赖关系 | 仿写优先级 |
| --- | --- | --- | --- | --- |
| {服务名称} | {模块名称} | {该模块完成的业务能力} | {依赖的服务/模块/基础能力} | {P0/P1/P2} |

## 3. 目录与文件概览

列出理解和仿写项目所需的关键目录与文件，不要罗列所有生成文件、依赖目录或无关资源。每一行都要说明它包含什么，以及为什么对理解业务、工程结构或仿写顺序有价值。若用途来自代码结构推断而非明确注释或文档，写明“根据代码结构推断”。

| 目录/文件 | 简要说明 | 仿写价值 | 代码证据 |
| --- | --- | --- | --- |
| `{目录或文件路径}` | {这个目录/文件包含的主要内容} | {它对理解服务边界、业务流程、数据结构、启动流程或接口实现的价值} | `{文件路径:行号}` |

## 4. 技术栈识别与开发流程建议

先说明这个项目建立在什么技术栈与工程工具之上，以及这些选择会如何影响仿写顺序。不要只写“用了什么”，还要写“仿写时应先做什么、后做什么”。

| 维度 | 识别结果 | 代码证据 | 对仿写的影响 |
| --- | --- | --- | --- |
| 语言 / 运行时 | {例如 Go / Node.js / Java} | `{文件路径:行号}` | {说明语言与运行时如何影响项目组织、构建方式或部署方式} |
| 核心框架 | {例如 go-zero / Gin / Spring Boot / NestJS} | `{文件路径:行号}` | {说明框架约束的目录结构、分层方式、生命周期或推荐写法} |
| 代码生成 / 合约工具 | {例如 goctl / protoc / ent / openapi-generator；若没有则写“未在代码中确认”} | `{文件路径:行号}` | {说明是否存在“先定义再生成”的开发方式} |
| 配置与初始化方式 | {例如 yaml + main/bootstrap + service context} | `{文件路径:行号}` | {说明仿写时应先补哪些配置、启动、依赖注入或上下文装配能力} |

如果框架存在明显的推荐开发流程，请按真实工程顺序写出来。例如 go-zero 的 API 类项目，通常可以按“数据模型 / `model` -> 生成配置 -> `.api` 定义 -> `goctl` 生成骨架 -> 初始化层 `main` / `svc` -> `logic` / `handler` 补充业务逻辑”的顺序说明。若其中某些步骤只是框架常见实践、未被当前项目代码直接证明，必须明确标注“未在代码中确认，为框架常见流程推断”。

## 5. 仿写建议路线 / 模块实施顺序

按工程实现顺序规划，而不是按源项目文件顺序抄写。排序时优先考虑：

- 被其他模块依赖的基础能力先写，例如配置、日志、统一响应、错误码、鉴权骨架、数据库连接、基础模型。
- 核心业务闭环需要的模块先写。
- 强依赖前置模块的功能后写。
- 管理、统计、优化、边缘场景等非核心能力后写。

| 顺序 | 实施模块/能力 | 前置依赖 | 为什么这样排序 | 完成标志 |
| --- | --- | --- | --- | --- |
| 1 | {模块或基础能力} | {无/依赖项} | {说明它为什么应该先实现，例如被多个模块依赖或支撑核心链路} | {如何判断这一阶段完成} |

## 6. 代码证据

| 结论 | 代码位置 |
| --- | --- |
| {结论} | `{文件路径:行号}` |
